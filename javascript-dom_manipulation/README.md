DOM Manipulation

To get index.html files to load correctly from VSCode for testing inside a Docker container

```bash
cd holbertonschool-higher_level_programming/javascript-dom_manipulation
python3 -m http.server 8000
```