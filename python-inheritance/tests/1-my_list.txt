    """
    Test the print_sorted function. 
    """

    """
    Import ``print_sorted`` from the ``1-my_list`` file
    >>> MyList = __import__('1-my_list').MyList

    Happy path - print array
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(2)
    >>> my_list.append(3)
    >>> my_list.append(5)
    >>> my_list.print_sorted()
    [1, 2, 3, 4, 5]

    Check for __str__
    >>> print(str(my_list))
    [1, 4, 2, 3, 5]

    Alternative prints as __str__
    >>> def __str__(self):
    ...    return [self]

    Check for negative number
    >>> my_list.append(-2)
    >>> my_list.print_sorted()
    [-2, 1, 2, 3, 4, 5]

    Check for empty list
    >>> empty_list = MyList()
    >>> empty_list.print_sorted()
    []

    """
